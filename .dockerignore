# Segredos
.env
.env.*
!.env.example

# Git
.git
.gitignore
.github

# Worktrees do Claude (symlinks de node_modules quebram o build context)
.claude/

# Python
__pycache__
*.py[cod]
*$py.class
.venv
venv/
.pytest_cache
.mypy_cache
.ruff_cache
.coverage

# Node (instalado dentro do container pelo `npm ci` do Dockerfile).
# `node_modules` sem `**/` casa SÓ na raiz — sem isto, o node_modules do runner
# Playwright (app/runners/legalone) entrava no build context: 4,75 GB por build
# e falha em `.bin/playwright` (symlink). Vale pro deploy do Coolify também.
**/node_modules
node_modules
frontend/node_modules
frontend/dist
frontend/bun.lockb

# Logs, outputs e runtime
*.log
backend-dev*.log
logs/
output/*.log
output/*.pid
output/*.err.log
tmp/

# Dados locais e planilhas de teste
data/
*.db
*.sqlite*
*.bak-*
entrada.xlsx
planilha_de_processos.xlsx

# Scripts de diagnóstico local
/test_*.py
/run_manual_sync.py
/inspect_lawsuit.py
/update_lawsuits.py
/test.py

# IDE / SO
.vscode
.idea
.DS_Store
Thumbs.db

# Documentação (não precisa no runtime)
docs/
*.md
!README.md

# Junk
Dockerfile copy.frontend
legal-one-firms-brazil-api.json
